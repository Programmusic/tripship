# Server
PORT=3000
NODE_ENV=development

# Auth — change in production!
JWT_SECRET=your-super-secret-jwt-key

# Database (optional — uses SQLite if not set)
# DATABASE_URL=postgresql://tripship:password@localhost:5432/tripship
